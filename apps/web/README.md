# Web

Shell React + TypeScript mínimo da fundação.

A interface atual prova o roteamento pelo Caddy e a comunicação com a API. Ela não é o design final. O material produzido no Google Stitch deverá ser incorporado em uma issue própria, preservando:

- chamadas à API sob `/api/v1`;
- acessibilidade e responsividade;
- estados explícitos de carregamento e erro;
- ausência de regras financeiras exclusivas no cliente.
