# Testes de sistema

Este diretório contém testes que atravessam mais de uma aplicação.

Os testes unitários permanecem próximos de cada app. O smoke test do Compose está em `tests/smoke/compose-smoke.sh`.
