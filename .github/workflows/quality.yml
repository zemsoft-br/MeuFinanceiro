name: Quality

on:
  pull_request:
    branches:
      - develop
      - main
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Mandatory quality gates
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout pull request head
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: |
            apps/api/pyproject.toml
            apps/worker/pyproject.toml
            infra/scripts/run-quality.py

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24.18.0'
          package-manager-cache: false

      - name: Cache npm downloads
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('apps/web/package.json', 'apps/web/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-

      - name: Validate DCO sign-offs
        if: github.event_name == 'pull_request'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          git fetch --no-tags origin "$BASE_SHA"
          python infra/scripts/check-dco.py "$BASE_SHA" "$HEAD_SHA"

      - name: Bootstrap missing frontend lockfile
        id: lockfile
        working-directory: apps/web
        shell: bash
        run: |
          if [[ -f package-lock.json ]]; then
            echo "generated=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          npm install --package-lock-only --ignore-scripts --no-audit --no-fund
          echo "generated=true" >> "$GITHUB_OUTPUT"

      - name: Upload generated frontend lockfile
        if: steps.lockfile.outputs.generated == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: web-package-lock
          path: apps/web/package-lock.json
          if-no-files-found: error
          retention-days: 1

      - name: Require committed frontend lockfile
        if: steps.lockfile.outputs.generated == 'true'
        run: |
          echo "apps/web/package-lock.json was generated and uploaded as the web-package-lock artifact."
          echo "Commit that file before the mandatory gates can pass."
          exit 1

      - name: Run local-equivalent quality suite
        run: python infra/scripts/run-quality.py --recreate
