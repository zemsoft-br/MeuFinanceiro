version: 2

updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore(deps)"

# Ecossistemas Python, npm e Docker serão adicionados pela issue #4
# quando os manifests e diretórios definitivos existirem.
